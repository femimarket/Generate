<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The generation Api talks to the network from native code. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ProjectService's documents root is self-configured by the api's own
         androidx.startup initializer (api ≥4.11.0); this library adds nothing. -->

</manifest>
